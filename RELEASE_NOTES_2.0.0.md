## Dicode Config Checker 2.0.0 Stable

نسخهٔ پایدار ۲ با موتور «تست واقعی» تازه، رابط بازطراحی‌شده و انتشار هماهنگ در چهار پلتفرم.

### موتور تست واقعی — بازنویسی از Python به C#

- برنامهٔ دسکتاپ کامل با C# و .NET 8 بازنویسی شد؛ مسیر اجرای پایتون حذف شد.
- کانفیگ‌ها صفحه‌بندی می‌شوند و هر صفحه فقط یک پردازش هستهٔ شبکه باز می‌کند؛ برای هر کانفیگ یک ورودی SOCKS ایزوله ساخته می‌شود و درخواست HTTP واقعی دقیقاً از داخل همان مسیر عبور می‌کند.
- نتیجه بر اساس median مرتب می‌شود؛ کمترین تأخیر، میانگین، تعداد تلاش و تعداد موفقیت نیز در گزارش می‌ماند.
- فقط موارد شکست‌خورده یک بار دیگر retry می‌شوند؛ موارد سالم بی‌دلیل دوباره تست نمی‌شوند.
- اندروید از سنجش تأخیر واقعی هستهٔ موبایل استفاده می‌کند و پروکسی‌های Telegram با تست TCP مستقل سنجیده می‌شوند و در گزارش اشتباه گرفته نمی‌شوند.
- پروتکل‌های VLESS ،VMess ،Trojan ،Shadowsocks ،Hysteria 2 و TUIC با دو هستهٔ مستقل پوشش داده می‌شوند.

### فناوری‌ها — بروز و پین‌شده

- Core A: Xray-core `v26.3.27` — آخرین نسخهٔ پایدار
- Core B: sing-box `1.13.19` — ارتقا از 1.13.15 با راستی‌آزمایی SHA256 برای هر چهار بسته
- GeoFiles نسخهٔ `202608252219` همراه بستهٔ Windows قرار می‌گیرد.
- هستهٔ موبایل Android روی commit ثابت ساخته و داخل APK بسته می‌شود.
- تمامی نسخه‌ها در workflow انتشار ثابت و هش‌شده‌اند تا build تکرارپذیر بماند.

### قابلیت‌ها — همهٔ نیازهای قبلی سر جایشان

- فونت فارسی وزیرمتن روی Windows، Linux، macOS و Android داخل بسته است و رابط روی همه سیستم‌ها یکسان دیده می‌شود.
- جداسازی کامل کانفیگ و پروکسی تلگرام: `sub.txt` و `proxy.txt` جدا تولید می‌شوند و صفحات جدا دارند.
- خروجی فقط کانفیگ یا فقط پروکسی با یک کلید قابل انتخاب است.
- تغییر نام خودکار کانفیگ‌ها با پیشوند دلخواه فعال/غیرفعال می‌شود.
- انتشار ساب اختصاصی GitHub بازگشت: پس از هر تست موفق، sub.txt و proxy.txt به‌صورت اتمیک منتشر و راستی‌آزمایی می‌شوند.
- تعداد دریافت از کانال‌های رتبهٔ اول و دوم به‌صورت مستقل کنترل می‌شود.
- عملیات طولانی قابل توقف است و پیشرفت مرحله‌ای نمایش داده می‌شود.

### رابط کاربری — مینیمال و دوپوسته

- Windows / Linux / macOS: رابط Avalonia فارسی راست‌به‌چپ با داشبورد، نتایج، تنظیمات؛ پوسته روشن، تیره و هماهنگ با سیستم.
- Android: پوسته روشن/تیره/سیستم با آیکون‌های مینیمال وکتور، ذخیرهٔ دائمی تنظیمات و فهرست کانال‌ها، و تایپوگرافی وزیرمتن.
- هر دو رابط سبک، تخت و بدون شلوغی طراحی شده‌اند.

| Platform | Package |
| --- | --- |
| Windows x64 | `DicodeConfigChecker-v2.0.0-windows-x64.zip` |
| Linux x64 | `DicodeConfigChecker-v2.0.0-linux-x86_64.tar.gz` |
| macOS Intel | `DicodeConfigChecker-v2.0.0-macos-x86_64.zip` |
| macOS Apple Silicon | `DicodeConfigChecker-v2.0.0-macos-arm64.zip` |
| Android | `DicodeConfigChecker-v2.0.0-android.apk` |

فایل‌های `SHA256SUMS.txt` و `provenance.json` برای کنترل صحت دانلودها ضمیمهٔ Release هستند. این نسخه پایدار است.

> برای اندازه‌گیری معتبر، VPN یا پراکسی دیگری که مسیر سیستم را تغییر می‌دهد پیش از تست خاموش کنید.
